# CHI-219 workstate — nisse CI/PR posture

Plan: `plans/2026-08-24-chi-219-ci-pr-posture.md` (approved, adversary-reviewed).
Branch: `chizhangucb/chi-219-nisse-ci-pytestlint-confidentiality-guard-tag-releases`.

## Goal
CI (pytest + ruff + confidentiality guard), tag releases, `.coderabbit.yaml`,
PR flow + branch protection mirroring chronicle, confirm egress pin.

## Done
- Ticket → In Progress.
- Plan written + adversarially reviewed + hardened.
- Feature branch created. Tools venv at scratchpad/venv (ruff 0.16.4, pytest 9.1.1).

## In flight
- Writing the deliverable files.

## Next
1. Write ci.yml, pyproject.toml, confidentiality_guard.py + test, release.yml, .coderabbit.yaml.
2. Local: pytest + ruff --fix + guard, all green.
3. Push via `egress git push`; open PR; CI green.
4. Merge PR#1 via `egress gh pr merge`.
5. Apply branch protection (body.json via --input; restrictions:null).
6. Tag v0.1.0. Confirm egress item 5 in writing. → In Review.

## Gotchas
- Guard must scan `git ls-files` only + exclude self (guard src, its test, plans/,
  plans/workstate/, archives/plans/). Match ONLY `/Users/chizhang`,`/home/chizhang`,
  `\Users\chizhang` — never bare `chizhang` (public handle; `chizhang-2` legit).
- Branch-protection PUT needs all 4 top-level fields incl `restrictions:null`; use --input.
- Required contexts = bare job ids `[test,lint,confidentiality]`; typo → perpetual pending.
- ruff/pytest not on machine python; use scratchpad venv.
