---
type: source
tags: []                # 3-5 canonical slugs from wiki/metadata/tag_registry.md
project:                # work | personal | health | life
created:
ingested:
origin:                 # URL or raw/ file path
via:                    # obsidian-clipper | parallel | webfetch | paste
retrieval:              # full | partial | excerpts
storage:                # verbatim | digest
class:                  # primary | external
confidential:           # empty, or a list: [finance] | [personnel] | [legal] | [finance, personnel] ...
distilled:              # empty = awaiting wiki-distill; set to date when distilled
triaged:                # empty = still in the triage queue (brainstorms and clippings only)
# recovered:            # only on retranscribed sources; a block with engine + date subkeys
---

# Context
- What this is:
- Why it entered the wiki:

<!-- Rule 7 callout goes here, and ONLY when the page carries a confidential: mark or a sensitive
     category surfaced and was ruled out. Nothing sensitive in the content = no callout at all.

> [!note] Rule 7 routing, set at ingest
> Which marks the page carries and why. Name what was considered and rejected too.
-->

# Summary (factual)

<!-- Thread under bold labels only when there are 3+ distinct topics; each label is a parent bullet
     ("- **Label**") with 2-space-indented children, no blank line between. Otherwise keep flat. -->

-

# Takeaways
-

# Signals
Primary distill input. Extract generously, with attribution; subtle or ambiguous signals included.

<!-- Format: - **[[target]]** | signal. Target is the page this most likely feeds;
     **target unclear** is allowed. A hint for wiki-distill, never a constraint. -->

- (None)

<!-- One `> [!warning] Capture caveats` callout goes here when any caveat applies. Blank line before
     and after the whole block; a bare `>` line between each labelled section. Bold labels inside in
     this order: **Retrieval:** (two sentences, hard cap), capture or attribution labels,
     **Name gaps:**. -->

# Distilled
Filled by wiki-distill: every wiki page this source's distillation touched, what changed there, and what landed.
Format: `- [[page]] | Evidence +N | one line of what landed`.
- (No durable updates.)
