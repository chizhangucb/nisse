# references/

Lookup artifacts you read to do work: your voice samples, frameworks, API guides (`<tool>-api.md`, saved once when you wire a tool), and `templates/` for doc scaffolds. Not accumulated records; that's `records/`.

Ships with:

- `batch-run-playbook.md`: craft rules for multi-ticket agent runs (right-sizing, review discipline, cost shape).
- `spoke-pattern.md`: how a second agent surface plugs into the hub, soul-floor template included.
- `templates/`: `source-page-meeting.md` + `source-page-general.md` (the wiki-ingest scaffolds; their inline comments are the per-section rules), `satellite-claude-md.md` (instantiate as any satellite's floor), `voice-template.md` (fill and save as `voice.md`).

Lifecycle: no dating. A file untouched for 90+ days with zero references from the rest of the repo is an archive candidate; hygiene proposes, you decide.
